# Legacy required status checks — kept so existing branch protection rules
# continue to pass. Can be removed once the branch protection rules are
# updated to reference the new workflow jobs in cpu-serial.yml,
# cpu-openmp.yml, gpu-cuda.yml, and gpu-hip.yml.
name: CI workflow (legacy)

on:
  pull_request:
    types: [opened, synchronize, reopened, labeled, ready_for_review]
    branches: ["master"]

jobs:
  build-cpu-serial-debug:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Replaced by 'CPU Serial / Build & Unit Tests' in cpu-serial.yml"

  build-cpu-openmp-debug:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Replaced by 'CPU OpenMP / Build & Unit Tests' in cpu-openmp.yml"

  build-cuda-debug:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Replaced by 'GPU CUDA / Compile' in gpu-cuda.yml"

  build-hip-debug:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Replaced by 'GPU HIP / Compile' in gpu-hip.yml"
